spring:
  application:
    name: bioimpedance-app

  datasource:
    url: jdbc:postgresql://ep-old-darkness-accnbfel-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_b6RUnXsM4wZE
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

stripe:
  secret-key: ${STRIPE_SECRET_KEY:}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET:}
  success-url: ${STRIPE_SUCCESS_URL:http://localhost:5173/settings?billing=success}
  cancel-url: ${STRIPE_CANCEL_URL:http://localhost:5173/settings?billing=cancelled}
  portal-return-url: ${STRIPE_PORTAL_RETURN_URL:http://localhost:5173/settings}
  prices:
    basic: ${STRIPE_PRICE_BASIC:}
    pro: ${STRIPE_PRICE_PRO:}
    studio: ${STRIPE_PRICE_STUDIO:}

billing:
  default-plan: ${BILLING_DEFAULT_PLAN:BASIC}

# Configurações opcionais úteis
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
